# Route Preference Models

这里预留路线偏好排序、价值评估、特征处理和训练推理相关模型算法代码。

建议后续按实际职责继续拆分：

- `features/`：特征读取、转换、标准化。
- `datasets/`：训练样本加载和切分。
- `ranking/`：路线排序模型。
- `value/`：路线价值判断或打分模型。
- `evaluation/`：离线评估指标和验证脚本。

